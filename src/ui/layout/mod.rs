//! Layout primitives: slot definitions, page widgets and bottom-player
//! presets. These types are pure data — the actual GTK containers live in
//! `ui::right_panel` and `ui::bottom_player`.

pub mod presets;
pub mod slots;
